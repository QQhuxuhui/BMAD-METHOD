# {算法/约束/目标/领域名称}

> 📝 本文档是V4.1标准模块模板，请根据实际内容填充各章节

## 元数据

```yaml
# 基本信息
所属类别: {精确算法|启发式算法|元启发式算法|混合算法|约束模式|目标函数|领域应用}
适用场景: {简要描述主要应用场景}
计算复杂度: {时间/空间复杂度}

# 归属信息
所属专家: @{调度算法|约束模式|目标优化|领域应用}专家智能体
专家库路径: @专家库/{专家库名称}/{类别}/{本文件名}.md

# 依赖关系
依赖模块:
  核心依赖:
    - @专家库/{库名}/{模块名}.md  # 必需的依赖

  场景依赖:
    {场景名称}:
      - @专家库/{库名}/{模块名}.md  # 特定场景的依赖

# 版本控制
version: v1.0
status: {已审核|待审核|实验性|已废弃}
generator: {人工编写|V4.0自动生成|V4.0辅助}
quality_score: {0.0-1.0, V4.0自动生成时填写}
最后更新: YYYY-MM-DD

# 更新日志
changelog:
  - v1.0 (YYYY-MM-DD): 初始版本
```

## 核心原理

### 理论基础

{描述理论来源、发展历史、核心思想}

**关键概念**：

- **概念1**: {定义}
- **概念2**: {定义}

### 核心机制

{详细描述工作机制、关键步骤}

**工作流程**：

1. **步骤1**: {描述}
2. **步骤2**: {描述}
3. **步骤3**: {描述}

## 实现框架

### 算法伪代码

```python
def algorithm_name(param1, param2, param3):
    """
    算法主函数

    参数:
        param1: {描述}
        param2: {描述}
        param3: {描述}

    返回:
        result: {描述}
    """
    # 初始化
    initialization()

    # 主循环
    while not termination_condition():
        # 核心操作
        operation1()
        operation2()

    # 返回结果
    return best_solution
```

### 参数配置指南

| 参数名称 | 推荐范围 | 默认值  | 影响说明               |
| -------- | -------- | ------- | ---------------------- |
| param1   | min-max  | default | {参数对算法性能的影响} |
| param2   | min-max  | default | {参数对算法性能的影响} |
| param3   | min-max  | default | {参数对算法性能的影响} |

**参数调优建议**：

- **param1**: {调优策略}
- **param2**: {调优策略}
- **param3**: {调优策略}

### 实现注意事项

1. **数据结构选择**: {建议的数据结构}
2. **性能优化点**: {关键优化位置}
3. **常见错误**: {容易出错的地方}

## 应用案例

### 案例1：{场景名称}

**场景描述**：
{详细描述应用场景、问题特征}

**问题建模**：

```yaml
决策变量: { 变量定义 }
约束条件:
  - { 约束1 }
  - { 约束2 }
目标函数: { 目标定义 }
```

**实施方案**：

```python
# 编码方式
encoding = {描述编码方式}

# 参数设置
config = {
    'param1': value1,
    'param2': value2,
}

# 约束处理
constraint_handling = @专家库/约束模式专家库/{约束名称}.md
```

**实施结果**：

- **优化效果**: {百分比或具体数值}
- **计算时间**: {耗时}
- **收敛特性**: {收敛代数、收敛曲线特点}

### 案例2：{场景名称}

{同上结构}

## 优势与局限

### 优势

- ✅ **优势1**: {详细说明}
- ✅ **优势2**: {详细说明}
- ✅ **优势3**: {详细说明}

### 局限

- ⚠️ **局限1**: {详细说明及应对策略}
- ⚠️ **局限2**: {详细说明及应对策略}
- ⚠️ **局限3**: {详细说明及应对策略}

### 适用性判断

**适合使用的情况**：

- {条件1}
- {条件2}

**不适合使用的情况**：

- {条件1}
- {条件2}

## 扩展变种

{如果有重要变种，描述变种机制}

### 变种1：{变种名称}

**核心改进**：{改进点}
**适用场景**：{特定场景}
**性能提升**：{量化提升}

### 变种2：{变种名称}

{同上结构}

## 调用接口

### 标准调用格式

```yaml
# 输入接口
输入:
  问题定义:
    - 决策变量: { 类型和范围 }
    - 约束条件: { 约束列表 }
    - 目标函数: { 目标定义 }

  算法参数:
    - param1: { 值或范围 }
    - param2: { 值或范围 }

  执行控制:
    - 终止条件: { 迭代次数|时间限制|质量阈值 }
    - 输出要求: { 详细程度 }

# 输出接口
输出:
  最优解:
    - 解向量: { 解的表示形式 }
    - 目标值: { 最优目标函数值 }
    - 可行性: { 是否满足所有约束 }

  过程信息:
    - 收敛曲线: { 每代/迭代的最优值序列 }
    - 统计信息: { 平均值、标准差、多样性指标 }

  性能指标:
    - 计算时间: { 秒 }
    - 迭代次数: { 实际迭代次数 }
    - 质量评估: { 与已知最优解的Gap }
```

### 与其他模块协作

**常见协作模式**：

```yaml
# 模式1：算法+约束
@本模块 + @专家库/约束模式专家库/{约束模块}.md
应用场景: {场景描述}

# 模式2：算法+目标
@本模块 + @专家库/目标函数专家库/{目标模块}.md
应用场景: {场景描述}

# 模式3：算法+领域
@本模块 + @专家库/领域应用专家库/{领域模块}.md
应用场景: {场景描述}
```

## V4.0 代码生成提示

### 核心类结构

```python
class AlgorithmName:
    """
    {算法名称}实现类

    该类是基于 @本模块 文档自动生成的代码框架。
    V4.0 Theory-to-Code 能力可以从此模板生成 80%+ 可用代码。
    """

    def __init__(self, config: AlgorithmConfig):
        """
        初始化算法

        Args:
            config: 算法配置对象
        """
        self.config = config
        self.initialize()

    def initialize(self):
        """初始化算法数据结构"""
        pass

    def execute(self, problem: Problem) -> Solution:
        """
        执行算法求解

        Args:
            problem: 问题定义对象

        Returns:
            solution: 求解结果对象
        """
        pass

    def _core_operation1(self):
        """核心操作1"""
        pass

    def _core_operation2(self):
        """核心操作2"""
        pass

    def _evaluate(self, solution):
        """评估解质量"""
        pass

    def _termination_check(self) -> bool:
        """检查终止条件"""
        pass
```

### 代码生成指导

**V4.0自动生成时应关注**：

1. **参数配置类**: 根据"参数配置指南"章节生成 `AlgorithmConfig` 类
2. **核心循环**: 根据"算法伪代码"章节生成主循环逻辑
3. **约束处理**: 根据"依赖模块"中的约束模块生成约束检查代码
4. **性能优化**: 根据"实现注意事项"生成优化代码

**测试用例生成**：

```python
def test_algorithm_basic():
    """基础功能测试"""
    # 根据"应用案例"章节生成测试用例
    pass

def test_algorithm_constraints():
    """约束处理测试"""
    # 根据依赖的约束模块生成约束测试
    pass

def test_algorithm_performance():
    """性能测试"""
    # 根据"参数配置指南"生成性能基准测试
    pass
```

## 相关资源

### 理论文献

**开创性论文**：

- {作者} ({年份}). "{论文标题}". {期刊/会议名称}.

**综述性论文**：

- {作者} ({年份}). "{论文标题}". {期刊/会议名称}.

**应用论文**（本领域代表性应用）：

- {作者} ({年份}). "{论文标题}". {期刊/会议名称}.

### 开源实现

**推荐库**：

- **库名1** ({语言}): {GitHub链接} - {简要说明}
- **库名2** ({语言}): {GitHub链接} - {简要说明}

### 学习资源

**在线教程**：

- {教程标题}: {链接}

**视频课程**：

- {课程标题}: {链接}

### 相关模块

**同类模块**（可替代或对比）：

- @专家库/{库名}/{模块名}.md - {简要对比}

**互补模块**（常配合使用）：

- @专家库/{库名}/{模块名}.md - {协作说明}

---

## 📋 模块维护信息

**创建人**: {姓名}
**审核人**: {姓名}
**最后编辑**: {YYYY-MM-DD}
**质量等级**: {A|B|C}
**使用频率**: {高|中|低}

**维护记录**：

```yaml
- date: YYYY-MM-DD
  action: { 创建|更新|审核|废弃 }
  description: { 变更说明 }
  author: { 姓名 }
```

---

**模板版本**: V4.1-Template-1.0
**模板更新**: 2024-03-20
