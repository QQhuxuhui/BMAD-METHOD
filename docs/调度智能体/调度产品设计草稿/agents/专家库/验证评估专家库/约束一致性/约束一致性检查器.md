---
module_name: 约束一致性检查器
category: 约束一致性
version: v1.0.0
updated: 2025-10-10
---

# 约束一致性检查器

## 目标

- 对比 TenElementModel 中的约束清单与代码中的实现痕迹，评估实现覆盖与一致性。

## 接口

```python
def check_constraint_consistency(code: str, model: dict) -> dict:
    """返回 {ok: bool, implemented: [str], missing: [str], consistency_score: float}"""
```

## 最小实现思路

- 基于关键字/函数名启发式匹配（如 `check_time_window`, `capacity_penalty`）。
- 提供可扩展的 `constraint_aliases` 词典与正则集合。
