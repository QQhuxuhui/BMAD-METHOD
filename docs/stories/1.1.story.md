# Story 1.1: 搭建LangGraph 1.0开发环境

## Status

**Draft**

## Story

**作为一个** BMAD开发者，
**我希望** 能够快速搭建LangGraph 1.0开发环境，
**以便于** 开始智能体集成开发工作

## Acceptance Criteria

1. 创建完整的Python 3.11+虚拟环境配置
2. 安装LangGraph 1.0.2及所有依赖包（包括langchain-agents替代prebuilt）
3. 配置PostgreSQL和Redis的Docker Compose开发环境
4. 验证LangGraph 1.0基础功能正常工作
5. 提供环境验证脚本和文档

## Tasks / Subtasks

- [ ] **Task 1: 创建Python虚拟环境** (AC: 1)
  - [ ] 使用conda创建名为`bmad-langgraph`的Python 3.11环境
  - [ ] 激活虚拟环境并验证Python版本
  - [ ] 记录环境创建命令到文档

- [ ] **Task 2: 准备依赖配置文件** (AC: 2)
  - [ ] 创建`backend/requirements.txt`文件
  - [ ] 添加核心依赖：LangGraph 1.0.2, LangChain Core 0.2.38+, LangGraph SDK 1.0.0+
  - [ ] 添加框架依赖：FastAPI 0.115.0+, Uvicorn 0.26.0+, LangServe 0.3.0+
  - [ ] 添加数据库依赖：psycopg 3.2.0+, LangGraph Checkpoint 2.0.23+
  - [ ] 添加工具依赖：httpx 0.25.0+, orjson 3.9.7+, tenacity 8.0.0+, cloudpickle 3.0.0+
  - [ ] 添加开发依赖：pytest 7.4+, structlog 24.1.0+

- [ ] **Task 3: 安装Python依赖** (AC: 2)
  - [ ] 切换到backend目录
  - [ ] 执行`pip install -r requirements.txt`
  - [ ] 验证关键包安装成功（特别是langchain-agents）
  - [ ] 记录安装过程中的任何警告或错误

- [ ] **Task 4: 配置Docker Compose环境** (AC: 3)
  - [ ] 创建`docker/docker-compose.dev.yml`文件
  - [ ] 配置PostgreSQL 16服务（端口5432）
  - [ ] 配置Redis 7服务（端口6379）
  - [ ] 设置数据持久化卷（postgres_data, redis_data）
  - [ ] 配置环境变量（POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD）

- [ ] **Task 5: 创建环境变量配置** (AC: 3)
  - [ ] 创建`.env.example`模板文件（项目根目录）
  - [ ] 定义数据库连接字符串：`DATABASE_URL`
  - [ ] 定义Redis连接字符串：`REDIS_URL`
  - [ ] 定义API密钥占位符（用于后续模型集成）
  - [ ] 添加配置说明注释

- [ ] **Task 6: 启动开发环境** (AC: 3, 4)
  - [ ] 启动Docker Compose：`docker-compose -f docker/docker-compose.dev.yml up -d`
  - [ ] 验证PostgreSQL容器运行状态
  - [ ] 验证Redis容器运行状态
  - [ ] 测试数据库连接（使用psql或Python脚本）
  - [ ] 测试Redis连接（使用redis-cli或Python脚本）

- [ ] **Task 7: 创建环境验证脚本** (AC: 4, 5)
  - [ ] 创建`scripts/verify_environment.py`脚本
  - [ ] 验证Python版本（>=3.11）
  - [ ] 验证关键包安装（LangGraph, FastAPI等）
  - [ ] 验证PostgreSQL连接
  - [ ] 验证Redis连接
  - [ ] 测试LangGraph基础功能（创建简单StateGraph）
  - [ ] 输出详细的验证报告

- [ ] **Task 8: 编写环境搭建文档** (AC: 5)
  - [ ] 创建`docs/setup-guide.md`文档
  - [ ] 记录完整的环境搭建步骤
  - [ ] 包含所有必要的命令和配置
  - [ ] 添加常见问题排查指南
  - [ ] 记录宿主机环境要求（conda, docker, docker-compose）

- [ ] **Task 9: 单元测试** (AC: 1, 2, 3, 4)
  - [ ] 创建`backend/tests/test_environment.py`
  - [ ] 测试Python环境配置
  - [ ] 测试依赖包导入
  - [ ] 测试数据库连接
  - [ ] 测试Redis连接
  - [ ] 确保测试覆盖率>80%

## Dev Notes

### Previous Story Insights

无（这是第一个故事）

### 技术栈要求

**Python环境** [Source: architecture/3-tech-stack.md]

- Python版本：3.11+（LangGraph CLI硬性要求）
- 环境管理：Conda（宿主机已配置）
- 环境名称建议：`bmad-langgraph`

**核心依赖包** [Source: architecture/3-tech-stack.md]

- LangGraph: 1.0.2（智能体工作流StateGraph编排）
- LangChain Core: 0.2.38+（智能体工具链）
- LangGraph SDK: 1.0.0+（客户端和工具）
- LangGraph Checkpoint: 2.0.23+（状态持久化）
- LangServe: 0.3.0+（自动API生成）
- FastAPI: 0.115.0+（RESTful API框架）
- Uvicorn: 0.26.0+（ASGI服务器）
- sse-starlette: 2.1.0（Server-Sent Events）
- httpx: 0.25.0+（异步HTTP客户端）

**数据库依赖** [Source: architecture/3-tech-stack.md]

- PostgreSQL: 16+（工作流状态持久化，LangGraph checkpoint后端）
- Redis: 7+（会话缓存和任务队列）
- psycopg: 3.2.0+（PostgreSQL Python驱动，支持连接池和async）

**工具包** [Source: architecture/3-tech-stack.md]

- orjson: 3.9.7+（高性能JSON序列化）
- tenacity: 8.0.0+（重试机制）
- cloudpickle: 3.0.0+（Python对象序列化，LangGraph checkpoint依赖）
- Pydantic: 2.10.0+（数据验证，FastAPI核心依赖）
- structlog: 24.1.0+（结构化日志）

**测试工具** [Source: architecture/3-tech-stack.md]

- Pytest: 7.4+（Python单元和集成测试）

**可观测性（可选）** [Source: architecture/3-tech-stack.md]

- LangSmith: 0.1.63+（LangGraph工作流追踪和调试）

### 项目结构

**目录组织** [Source: architecture/10-unified-project-structure.md]

```
BMAD-METHOD/
├── backend/                    # 后端Python代码
│   ├── langgraph_service/      # 工作流服务主目录
│   │   ├── main.py             # FastAPI应用入口
│   │   ├── workflow_engine.py  # 工作流引擎
│   │   └── agents/             # 八大智能体
│   ├── model_adapters/         # 模型适配器
│   ├── shared/                 # 后端共享模块
│   │   ├── database/           # 数据库工具
│   │   └── utils/              # 工具函数
│   ├── tests/                  # 后端测试
│   └── requirements.txt        # Python依赖配置
├── docker/                     # Docker配置
│   └── docker-compose.dev.yml  # 开发环境编排
├── docs/                       # 文档目录
├── scripts/                    # 脚本目录
└── .env.example                # 环境变量模板
```

### 开发工作流

**环境搭建流程** [Source: architecture/11-development-workflow.md]

```bash
# 1. 激活nvm环境（宿主机要求）
source ~/.nvm/nvm.sh

# 2. 创建Python环境（使用conda）
conda create -n bmad-langgraph python=3.11
conda activate bmad-langgraph

# 3. 安装依赖
cd backend
pip install -r requirements.txt

# 4. 启动数据库服务
cd ../docker
docker-compose -f docker-compose.dev.yml up -d postgres redis

# 5. 配置环境变量
cp ../.env.example ../.env
# 编辑.env配置数据库和API密钥
```

**开发命令** [Source: architecture/11-development-workflow.md]

```bash
# 启动后端服务
cd backend
uvicorn langgraph_service.main:app --reload --port 8000

# 运行测试
cd backend && pytest tests/
```

### Docker Compose配置

**开发环境配置要求** [Source: architecture/12-deployment-architecture.md]

```yaml
# docker/docker-compose.dev.yml 基本结构
version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'

  redis:
    image: redis:7-alpine
    volumes:
      - redis_data:/data
    ports:
      - '6379:6379'

volumes:
  postgres_data:
  redis_data:
```

### 环境变量配置

**.env 文件必需配置** [Source: architecture/11-development-workflow.md, 12-deployment-architecture.md]

```bash
# 数据库配置
POSTGRES_DB=bmad_langgraph_dev
POSTGRES_USER=bmad_user
POSTGRES_PASSWORD=your_secure_password
DATABASE_URL=postgresql://bmad_user:your_secure_password@localhost:5432/bmad_langgraph_dev

# Redis配置
REDIS_URL=redis://localhost:6379/0

# API密钥（Phase 1暂时可为空，后续故事中配置）
OPENAI_API_KEY=
QWEN_API_KEY=
GLM_API_KEY=
DEEPSEEK_API_KEY=
```

### 特别注意事项

**重要约束** [Source: architecture/3-tech-stack.md]

1. Python版本必须是3.11或更高（LangGraph CLI硬性要求）
2. 必须安装langchain-agents包（替代已弃用的prebuilt模块）
3. PostgreSQL用作LangGraph checkpoint持久化后端
4. 所有异步操作必须使用async/await模式

**宿主机环境要求** [Source: CLAUDE.md]

- Conda：Python环境管理（已配置）
- Docker：容器技术（已配置）
- Docker Compose：容器编排（已配置）
- nvm：Node.js版本管理（需要在使用前执行`source ~/.nvm/nvm.sh`）

**依赖包特殊说明**

- LangGraph 1.0.2是稳定版本，提供内置持久化和Human-in-Loop特性
- langchain-agents是新的API，替代了prebuilt模块
- psycopg 3.x支持连接池和异步操作
- cloudpickle是LangGraph checkpoint的必需依赖

### Testing

**测试策略** [Source: architecture/14-testing-strategy.md]

- 测试文件位置：`backend/tests/`
- 测试框架：Pytest 7.4+
- 覆盖率目标：后端单元测试 >80%
- 测试金字塔：单元测试35%，集成测试25%，E2E测试5%

**测试要求**

- 创建`backend/tests/test_environment.py`
- 测试Python环境配置
- 测试关键依赖包导入
- 测试数据库连接
- 测试Redis连接
- 使用pytest运行所有测试

**命名约定** [Source: architecture/15-coding-standards.md]

- 测试文件：`test_*.py`
- 数据库表：snake_case（例如：`agent_executions`）
- API路由：kebab-case（例如：`/api/user-profile`）

## Change Log

| Date       | Version | Description  | Author             |
| ---------- | ------- | ------------ | ------------------ |
| 2025-11-04 | 1.0     | 初始故事创建 | Bob (Scrum Master) |

## Dev Agent Record

_此部分在开发实施过程中由开发智能体填写_

### Agent Model Used

_待开发智能体记录_

### Debug Log References

_待开发智能体记录_

### Completion Notes

_待开发智能体记录_

### File List

_待开发智能体记录_

## QA Results

_此部分在QA审查时由QA智能体填写_
