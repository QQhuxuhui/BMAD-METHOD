# Story 1.5.2: StateGraph编排和Checkpoint持久化

## Status

**Draft**

## Story

**作为一个** BMAD开发者，
**我希望** 将八大智能体节点编排到LangGraph StateGraph中，并配置PostgreSQL checkpoint持久化，
**以便于** 实现完整的工作流执行和状态管理

## Acceptance Criteria

1. StateGraph成功编译，包含所有8个agent节点和3个approval节点
2. 条件边路由逻辑正确，Phase 0-4流转无误
3. PostgreSQL Checkpoint自动保存和恢复
4. 支持从YAML配置文件加载智能体配置
5. 工作流可以正常执行（不含HITL交互）
6. Checkpoint数据完整性验证通过

## Tasks / Subtasks

- [ ] **Task 1: 实现StateGraph编排器** (AC: 1, 2)
  - [ ] 创建`backend/app/core/langgraph/workflow.py`
  - [ ] 导入所有agent节点函数
  - [ ] 创建StateGraph实例
  - [ ] 添加8个agent节点到图中
    ```python
    workflow.add_node("orchestrator", orchestrator_node)
    workflow.add_node("algorithm", algorithm_expert_node)
    workflow.add_node("constraint", constraint_expert_node)
    workflow.add_node("objective", objective_expert_node)
    workflow.add_node("domain", domain_expert_node)
    workflow.add_node("code_impl", code_impl_expert_node)
    workflow.add_node("extension", extension_expert_node)
    workflow.add_node("quality", quality_expert_node)
    ```
  - [ ] 添加3个approval节点（P1, P2, P2.5）
  - [ ] 设置入口点（orchestrator）
  - [ ] 添加顺序边（Phase内的节点连接）
    ```python
    # Phase 0
    workflow.set_entry_point("orchestrator")
    # Phase 1
    workflow.add_edge("orchestrator", "algorithm")
    workflow.add_edge("algorithm", "constraint")
    workflow.add_edge("constraint", "objective")
    # 等等...
    ```
  - [ ] 添加条件边（Phase间的路由）
    ```python
    workflow.add_conditional_edges(
        "p1_approval",
        route_after_p1_approval,
        {"approved": "domain", "rejected": "algorithm", "__end__": END}
    )
    ```

- [ ] **Task 2: 实现Approval节点（HITL占位符）** (AC: 2)
  - [ ] 创建`backend/app/core/langgraph/agents/approval_nodes.py`
  - [ ] 实现p1_approval_node
    - 设置pending_approval=True
    - 设置approval_point='P1'
    - 返回状态（触发checkpoint保存）
  - [ ] 实现p2_conflict_node（可选）
  - [ ] 实现p25_approval_node
  - [ ] 注意：此Story只实现占位符，真正的HITL交互在Story 1.5.3实现

- [ ] **Task 3: 配置PostgreSQL Checkpoint持久化** (AC: 3, 6)
  - [ ] 安装langgraph-checkpoint-postgres包（验证版本2.0.19+）
  - [ ] 在config.py添加checkpoint配置
    ```python
    CHECKPOINT_DB_URL = settings.database_url
    CHECKPOINT_TABLES = ["checkpoints", "checkpoint_blobs", "checkpoint_writes"]
    ```
  - [ ] 创建checkpoint数据库初始化函数

    ```python
    from langgraph.checkpoint.postgres.aio import AsyncPostgresSaver
    from psycopg_pool import AsyncConnectionPool

    async def create_checkpointer():
        pool = AsyncConnectionPool(CHECKPOINT_DB_URL)
        await pool.open()
        saver = AsyncPostgresSaver(pool)
        await saver.setup()  # 创建表结构
        return saver
    ```

  - [ ] 在workflow编译时配置checkpointer
    ```python
    checkpointer = await create_checkpointer()
    app = workflow.compile(checkpointer=checkpointer)
    ```
  - [ ] 验证checkpoint表创建成功
  - [ ] 测试checkpoint保存和加载

- [ ] **Task 4: 实现工作流配置兼容性适配** (AC: 4)
  - [ ] 创建`backend/app/core/langgraph/config_loader.py`
  - [ ] 实现YAML配置加载函数
    ```python
    def load_workflow_config(config_path: str) -> Dict:
        with open(config_path) as f:
            return yaml.safe_load(f)
    ```
  - [ ] 实现配置映射函数
    ```python
    def map_yaml_to_nodes(config: Dict) -> Dict[str, Callable]:
        # 根据YAML配置返回节点映射
        pass
    ```
  - [ ] 支持动态加载Prompt模板路径
  - [ ] 支持配置模型选择（per-agent model）
  - [ ] 向后兼容旧版workflow.yaml格式
  - [ ] 编写配置迁移指南

- [ ] **Task 5: 创建工作流执行函数** (AC: 5)
  - [ ] 在workflow.py实现`create_bmad_workflow()`
    ```python
    async def create_bmad_workflow(
        config_path: Optional[str] = None
    ) -> CompiledStateGraph:
        # 加载配置（可选）
        # 创建StateGraph
        # 添加节点和边
        # 配置checkpointer
        # 编译并返回
        pass
    ```
  - [ ] 实现`run_workflow()`执行函数
    ```python
    async def run_workflow(
        problem_description: str,
        domain: Optional[str] = None,
        constraints: Optional[List[str]] = None,
        thread_id: Optional[str] = None
    ) -> WorkflowState:
        # 创建初始状态
        # 执行工作流
        # 返回最终状态
        pass
    ```
  - [ ] 添加错误处理和超时控制

- [ ] **Task 6: 集成测试** (AC: 1-5)
  - [ ] 创建`tests/integration/test_workflow_compilation.py`
  - [ ] 测试StateGraph编译成功
  - [ ] 测试工作流执行（简单场景）
    - 创建初始状态
    - 执行工作流
    - 验证经过所有Phase
    - 验证最终输出
  - [ ] 测试checkpoint保存
    - 执行部分工作流
    - 验证checkpoint存在
    - 从checkpoint恢复
    - 继续执行
  - [ ] 测试YAML配置加载
  - [ ] 测试条件边路由（不同路径）

- [ ] **Task 7: 性能和稳定性测试** (AC: 6)
  - [ ] 测试并发执行（3个工作流同时运行）
  - [ ] 测试长时间运行（模拟复杂问题）
  - [ ] 测试checkpoint数据完整性
    - 保存后立即恢复，验证状态一致
    - 验证消息历史完整
  - [ ] 测试异常恢复（模拟节点失败）
  - [ ] 内存泄漏检查

## Dev Notes

### StateGraph构建示例

```python
from langgraph.graph import StateGraph, END
from backend.app.core.langgraph.state import WorkflowState
from backend.app.core.langgraph.agents import *
from backend.app.core.langgraph.routers import *

async def create_bmad_workflow():
    """创建BMAD工作流"""
    workflow = StateGraph(WorkflowState)

    # 添加agent节点
    workflow.add_node("orchestrator", orchestrator_node)
    workflow.add_node("algorithm", algorithm_expert_node)
    workflow.add_node("constraint", constraint_expert_node)
    workflow.add_node("objective", objective_expert_node)
    workflow.add_node("domain", domain_expert_node)
    workflow.add_node("code_impl", code_impl_expert_node)
    workflow.add_node("extension", extension_expert_node)
    workflow.add_node("quality", quality_expert_node)

    # 添加approval节点
    workflow.add_node("p1_approval", p1_approval_node)
    workflow.add_node("p25_approval", p25_approval_node)

    # 设置入口点
    workflow.set_entry_point("orchestrator")

    # Phase 0 → Phase 1
    workflow.add_edge("orchestrator", "algorithm")
    workflow.add_edge("algorithm", "constraint")
    workflow.add_edge("constraint", "objective")

    # Phase 1 → P1 Approval
    workflow.add_edge("objective", "p1_approval")
    workflow.add_conditional_edges(
        "p1_approval",
        route_after_p1_approval,
        {
            "approved": "domain",
            "rejected": "algorithm",
            "__end__": END
        }
    )

    # Phase 2 → Phase 3
    workflow.add_edge("domain", "code_impl")
    workflow.add_edge("code_impl", "extension")

    # Phase 3 → P2.5 Approval
    workflow.add_edge("extension", "p25_approval")
    workflow.add_conditional_edges(
        "p25_approval",
        route_after_p25_approval,
        {
            "approved": "quality",
            "rejected": "code_impl",
            "__end__": END
        }
    )

    # Phase 4 → End
    workflow.add_edge("quality", END)

    # 配置checkpointer
    checkpointer = await create_checkpointer()

    return workflow.compile(checkpointer=checkpointer)
```

### Checkpoint使用模式

```python
# 执行工作流（自动保存checkpoint）
config = {"configurable": {"thread_id": "unique-id"}}
async for event in workflow.astream(initial_state, config):
    print(event)  # 每个节点输出后自动保存checkpoint

# 从checkpoint恢复
state = await workflow.aget_state(config)
print(state.values)  # 最新状态
print(state.next)    # 下一个要执行的节点

# 继续执行
async for event in workflow.astream(None, config):  # None = 使用当前状态
    print(event)
```

### 配置文件格式

```yaml
# workflow.yaml
workflow:
  name: 'BMAD Eight-Agent Workflow'
  version: '1.0'

agents:
  orchestrator:
    prompt_template: 'prompts/orchestrator.md'
    model: 'qwen3-30b' # 可选：agent特定模型
    temperature: 0.7

  algorithm:
    prompt_template: 'prompts/algorithm.md'

  # ... 其他agents

phases:
  - id: 'P0'
    agents: ['orchestrator']

  - id: 'P1'
    agents: ['algorithm', 'constraint', 'objective']
    approval_required: true
    approval_point: 'P1'

  # ... 其他phases

checkpointer:
  enabled: true
  backend: 'postgresql'
```

### 技术要点

**依赖版本**:

- langgraph >= 0.4.1
- langgraph-checkpoint-postgres >= 2.0.19
- psycopg-pool >= 3.2.0

**数据库表结构** (自动创建):

```sql
CREATE TABLE checkpoints (
    thread_id TEXT NOT NULL,
    checkpoint_ns TEXT NOT NULL,
    checkpoint_id TEXT NOT NULL,
    parent_checkpoint_id TEXT,
    type TEXT,
    checkpoint JSONB NOT NULL,
    metadata JSONB NOT NULL,
    PRIMARY KEY (thread_id, checkpoint_ns, checkpoint_id)
);

CREATE TABLE checkpoint_blobs (
    thread_id TEXT NOT NULL,
    checkpoint_ns TEXT NOT NULL,
    channel TEXT NOT NULL,
    version TEXT NOT NULL,
    type TEXT NOT NULL,
    blob BYTEA,
    PRIMARY KEY (thread_id, checkpoint_ns, channel, version)
);

CREATE TABLE checkpoint_writes (
    thread_id TEXT NOT NULL,
    checkpoint_ns TEXT NOT NULL,
    checkpoint_id TEXT NOT NULL,
    task_id TEXT NOT NULL,
    idx INTEGER NOT NULL,
    channel TEXT NOT NULL,
    type TEXT,
    blob BYTEA NOT NULL,
    PRIMARY KEY (thread_id, checkpoint_ns, checkpoint_id, task_id, idx)
);
```

**Thread ID生成**:

```python
import uuid

thread_id = str(uuid.uuid4())  # 防止枚举攻击
```

**错误处理**:

- Checkpoint保存失败：记录错误但继续执行
- 节点执行失败：保存checkpoint后抛出异常
- 超时控制：单个节点30秒，整个工作流60分钟

## Change Log

| Date       | Version | Description         | Author |
| ---------- | ------- | ------------------- | ------ |
| 2025-11-05 | 1.0     | 创建Story 1.5.2草稿 | James  |

## Dev Agent Record

### Agent Model Used

待实现

### Debug Log References

待实现

### Completion Notes

待实现

### File List

待实现

## QA Results

待实现
