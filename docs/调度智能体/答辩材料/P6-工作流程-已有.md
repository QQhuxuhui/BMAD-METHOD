# P6 · 工作流程（用户已有）

> 创新案例答辩PPT · 第 6 / 9 页 · 预计讲述时长 45 秒

---

## 🎯 页面定位

**这一页由用户已经设计完成，保持原样**。作用是承前启后：

- **承前**（P5 架构图）：架构讲清楚后，这里展示"这套架构是怎么运转起来的"
- **启后**（P7 飞轮页）：流程讲完后，自然过渡到"这个流程在飞轮效应下越转越强"

---

## 📌 内容说明

> 此页内容由用户自行设计完成，展示**智能体团队的工作流程**（Phase 0-4 编排 / 专家协作时序等）。
>
> **本答辩 PPT 的其他 8 页（P1-P5、P7-P9）已围绕这一页的定位做了精心衔接**——P5 讲架构 / P6 讲运转 / P7 讲演进，三页递进、逻辑闭环。

---

## 🎨 视觉意境描述（投喂 flow 平台 · 风格基调建议）

> **特殊说明**
>
> 本页布局由用户自行设计，本节给 flow 平台的指引重点是 —— **风格基调对齐**，而不是画面构图。目标是确保本页与前后 8 页**视觉同源**，避免成为整套答辩中"视觉断裂的一页"。
>
> **整体氛围（与前后页一脉相承）**
>
> 这是承前启后的一页：**承 P5 架构图**（讲清楚团队结构）、**启 P7 飞轮**（讲清楚为什么越用越强）。本页讲的是"**团队怎么协作跑通一个真实任务**"——视觉气质应该是**冷静的工程感**：流程清晰、节点分明、状态可见、关键点可控。
>
> 整页继承前后页的纯白底质感，所有元素统一在白色画布上自由呼吸。
>
> **风格基调（强烈建议遵守）**
>
> - **背景**：纯白 `#FFFFFF` —— 与全场 8 页一致
> - **主文字**：深墨灰 `#1A202C` —— 标题、关键节点名
> - **副文字**：中灰 `#4A5568` —— 描述、注释
> - **强调色**：深蓝 `#1A365D` —— Phase 编号、关键节点高亮、流程主线箭头
> - **分割线**：浅灰 `#E2E8F0` —— 1px 细线
> - **正面成效色**：墨绿 `#2F855A` —— 例如"代码可用率 92%"、"零漂移"等
> - **警示色**：砖红 `#C53030` —— 仅在需要标注问题/异常分支时使用
> - **字体**：中文思源黑体 / 微软雅黑（兜底）；英文 Inter / Helvetica Neue（兜底）
>
> **关键流程要素（建议在画面中体现）**
>
> 流程图至少应该能让评委一眼读到以下 4 个关键特性（这 4 项与全场答辩叙事强相关）：
>
> 1. **Phase 0-4 全流程编排**——从需求到交付的结构化流程（5 个 Phase 节点串联，与 P5 总指挥 "Phase 0-4 编排" 呼应）
> 2. **双模式交互**——专家模式 A / 业务模式 B，适配不同用户群（视觉上可用左右分支或图标区分）
> 3. **Human-in-the-Loop**——5 级触发规则在关键决策点引入人工确认（视觉上可用"人形 icon + 暂停符号"标识关键节点）
> 4. **状态持久化 + 断点续传**——长流程可靠保障（视觉上可用"硬盘 icon"标识每 Phase 强制落盘节点；与 P4 反幻觉三件套呼应）
>
> **七专家在流程中的分工**（与 P5 角色名严格一致）
>
> 流程图中如出现专家角色，必须使用与 P5 完全一致的中文姓名：**赵领域 / 李严谨 / 王目标 / 张效率 / 孙质量 / 钱扩展 + Orchestrator 总指挥**——不要临时改名，不要换 emoji 风格。
>
> **与前后页的视觉呼应（关键）**
>
> - **承 P5**：保留 P5 的 7 位专家角色 + 角色 emoji + 颜色体系
> - **启 P7**：若流程图含"沉淀 / 实战 / 优化 / 复用"等循环元素，可使用与 P7 相同的环形或箭头符号做视觉呼应——这是把"流程跑得越多→飞轮越强"这个叙事，通过视觉语言提前埋下伏笔
>
> **图标风格**
>
> 线框 / 极简 icon（Tabler Icons / Feather Icons 风格），避免卡通化、避免色块图标。所有 icon 风格必须与 P4/P5/P7 一致。
>
> **严禁**
>
> 渐变背景 / 装饰花纹 / 立体阴影 / 卡通插画 / 拟人化 3D 形象 / 与前后页不一致的色系 / 全新引入的色彩或字体。**整体原则**：如原设计已定型难以全部调整，**至少把背景、字体、配色三项与全场 8 页对齐**——评委会自动把本页归入"同一套 PPT"。
>
> **🚨 语言硬性约束**
>
> 画面所有可见文案 **必须使用中文**。**唯一豁免**：专业术语（如 `Phase 0-4` · `Human-in-the-Loop` · `Agent` · `AI` · `Orchestrator` · `QA` 等）以及中文角色名（赵领域 / 李严谨 / 王目标 / 张效率 / 孙质量 / 钱扩展）。**禁止**任何可用中文等价表达的英文（如不要写 "Workflow / Process / Step 1"，必须用「工作流程 / 步骤一」；不要写 "Expert Mode A / Business Mode B"，必须用「专家模式 A / 业务模式 B」；除非"Phase 0-4"作为术语保留，否则尽量用「阶段一/二/三/四」等表达）。

---

## 🎨 样式说明

> 本页由用户原设计保留。**为确保整套答辩视觉统一，强烈建议此页与其他 8 页保持白色系商务正式风格一致**。

**建议对齐的要点**

- **背景**：纯白 `#FFFFFF`
- **主文字**：深墨灰 `#1A202C`
- **强调色**：深蓝 `#1A365D`（用于关键节点 / 专家名称 / 数字）
- **分割线**：浅灰 `#E2E8F0`（1px 细线）
- **字体**：中文思源黑体 / 微软雅黑；英文 Inter / Helvetica Neue
- **emoji 使用**：克制——保留功能性标识（如专家图标 🏢🔒🎯⚙️），删除装饰性表情
- **图示风格**：线框 / 极简 icon（Feather / Tabler 风格），避免卡通化

**文案语言规范**

- **除专业术语外，全文必须使用中文**（含标题、正文、讲词、图示注释、注脚）
- **可保留原文的专业术语**：如 `Agent as Doc` · `Sidecar` · `Token` · `Guardrails` · `Prompt` · `Markdown` · `Phase 0-4` · `Todo` · `QA` · `Git` · `Orchestrator` 等
- **数字与单位**：业务量用中文单位（"500 台"、"100+ 模板"），技术度量保留原单位（"50K tokens"、"15px"）
- **字体 / 产品 / 软件包名**保留原写法（思源黑体、Inter、Tabler Icons、Feather Icons、BMAD）

**与前后页的视觉衔接**

- **承接 P5 架构图**：P5 的 7 位专家名 + 角色颜色体系，在本页应保持一致（不要临时换色或换字体）
- **启后 P7 飞轮**：若流程图有"沉淀 / 实战 / 优化 / 复用"等循环元素，可用和 P7 相同的环形或箭头符号作视觉呼应
- **整体原则**：如原设计已定型难以调整，**答辩时视觉整体感仍比单页追求完美更重要**——其他 8 页都对齐后，评委会自动把 P6 归入"同一套 PPT"

---

## 🎤 口头讲词提示（~45 秒 · 建议主讲人围绕该页内容补充）

建议在已有内容基础上，围绕以下几个关键点组织讲词：

1. **承接 P5**："刚才讲的是团队结构，这一页讲的是团队怎么协作跑通一个真实任务"
2. **亮出关键特性**（建议提到至少 2 项）：
   - **Phase 0-4 全流程编排**（从需求到交付的结构化流程）
   - **双模式交互**（专家模式 A / 业务模式 B 适配不同用户群）
   - **Human-in-the-Loop**（5 级触发规则在关键决策点引入人工确认）
   - **状态持久化 + 断点续传**（长流程可靠保障）
3. **引出 P7**：以一句衔接词结束，比如——"这套流程跑得越多，专家库就越厚——这就是我们的第七页要讲的飞轮效应"

---

## 🔗 与其他页的关联

| 页  | 关系                                                                 |
| --- | -------------------------------------------------------------------- |
| P4  | 反幻觉三件套（Todo / Guardrails / 状态持久化）在此页流程中真正起作用 |
| P5  | 架构图中的七专家，在此页按流程分工协作                               |
| P7  | 此页展示的"知识调用"过程，正是飞轮第二环"实战"                       |

---

## 📐 flow 平台投喂提示词（一键复制 · 风格对齐版）

> **特殊说明**：本页布局以用户已有设计为准。本提示词只用于**风格对齐**——确保本页与全场 8 页视觉同源。
>
> 帮我**调整**一页 **16:9 商务正式 PPT**（主题：调度智能体的工作流程）的视觉风格，使其与全套答辩 PPT 视觉一致。
>
> **风格关键词**：冷静工程感 · 流程清晰 · 节点分明 · 状态可见 · 与前后页（P5 架构图 / P7 飞轮）视觉同源。
>
> **必须对齐的视觉规范**：
>
> 1. **配色（与全场 8 页严格统一）**：
>    - 背景：纯白 `#FFFFFF`
>    - 主文字：深墨灰 `#1A202C`
>    - 副文字：中灰 `#4A5568`
>    - 强调色：深蓝 `#1A365D`（流程主线、Phase 编号、关键节点）
>    - 分割线：浅灰 `#E2E8F0`
>    - 正面成效：墨绿 `#2F855A`
>    - 警示分支（如需）：砖红 `#C53030`
>    - **禁止引入新色彩**
> 2. **字体**：中文思源黑体 / 微软雅黑（兜底）；英文 Inter / Helvetica Neue（兜底）。
> 3. **图标风格**：Tabler Icons / Feather Icons 极简线框，与 P4/P5/P7 同风格。**禁止卡通化、禁止 3D**。
> 4. **关键流程要素必须出现**（这 4 项与全场叙事强相关）：
>    - **Phase 0-4 全流程编排**（5 个阶段节点串联）
>    - **双模式交互**：专家模式 A / 业务模式 B
>    - **Human-in-the-Loop**：5 级触发规则在关键决策点引入人工确认
>    - **状态持久化 + 断点续传**：每 Phase 强制落盘
> 5. **七专家中文姓名严格保留**：流程中如涉及专家角色，必须用 **赵领域 / 李严谨 / 王目标 / 张效率 / 孙质量 / 钱扩展 + Orchestrator 总指挥**，与 P5 完全一致。
> 6. **与前后页的视觉呼应**：
>    - 承 P5：专家盒子 / 角色 emoji / 颜色体系一致
>    - 启 P7：若流程含循环元素，使用与 P7 飞轮相同的环形/箭头符号做视觉呼应
>
> **严禁**：渐变背景 / 装饰花纹 / 立体阴影 / 卡通插画 / 拟人化 3D 形象 / 与前后页不一致的色系或字体。
>
> **🚨 语言硬性约束（最高优先级 · 不可违背）**：
>
> 生成结果中所有可见文案 **必须使用中文**。仅以下情况可保留原文：
>
> - **专业术语**：`Phase 0-4` · `Human-in-the-Loop` · `Agent` · `AI` · `Orchestrator` · `QA` · `Token` · `tokens` 等
> - **字体 / 产品 / 软件包名**：思源黑体、Inter、Tabler Icons、Feather Icons、BMAD 等
> - **中文角色名必须保留**：赵领域 / 李严谨 / 王目标 / 张效率 / 孙质量 / 钱扩展
>
> **任何可用中文等价表达的英文一律禁止**——若你输出了非术语英文（如 "Workflow / Process / Step 1 / Mode A / Mode B / Decision Point"），属违规，必须用中文重写为「工作流程 / 步骤一 / 专家模式 A / 业务模式 B / 决策点」等。

---

**上一页**：[P5 · 七专家协作架构](./P5-七专家协作架构.md) | **下一页**：[P7 · 专家经验飞轮](./P7-专家经验飞轮.md)
